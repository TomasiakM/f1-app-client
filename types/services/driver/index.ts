export interface IDriver {
    id: string;
    firstName: string;
    lastName: string;
    image: string;
    dateOfBirth: string;
    countryCode: string;
    descriptionHtml: string;
    slug: string;
}